<h1><%= link_to @song.title, song_path(@song) if @song.artist %> (<%= link_to "Edit Song", edit_artist_song_path(@song.artist, @song) if @song.artist %>)</h1>
<p> by <%= display_artist(@song) %></p>

<%= link_to "All Songs", songs_path %>
